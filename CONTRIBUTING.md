# 贡献指南

欢迎提交问题报告和改进建议。

## 开发环境

- Windows 10/11
- Python 3.10 或更高版本
- 已安装并使用过 Codex 的开发机可用于手动验证；单元测试不应依赖真实账号

运行测试：

```powershell
py -3 -m unittest discover -s . -p "test_*.py" -v
```

## 提交改动

1. 先创建分支，并说明改动目的。
2. 为解析、显示或刷新逻辑补充测试。
3. 不要在代码、日志、截图或测试数据中写入 Token、Cookie 或个人账号信息。
4. 提交前检查 `git diff` 和 `git diff --cached`。
5. Pull Request 请描述复现步骤、测试结果和兼容性影响。

## 兼容性原则

额度接口是非公开接口，字段可能变化。解析器应优先容错并保留旧格式兼容；自动化测试应使用脱敏的固定样例，不应直接请求线上接口。
