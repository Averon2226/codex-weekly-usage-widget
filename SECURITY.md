# 安全说明

## 不要提交敏感信息

本项目会读取本机 Codex 登录态文件中的访问令牌。请勿把以下内容提交到 GitHub、Issue 或 Pull Request：

- `auth.json`、访问令牌或 Cookie
- `logs_2.sqlite`、日志和账号截图
- 任何包含账号、邮箱或请求头的调试文件

仓库的 `.gitignore` 已覆盖常见的凭据、数据库和日志文件，但提交前仍应检查 `git diff --cached`。

## 报告漏洞

如果发现令牌泄露、敏感数据被输出或其他安全问题，请不要公开粘贴凭据。请使用 GitHub 的私有安全报告功能，或先删除敏感内容后再提交 Issue。

如果令牌曾被意外提交，应立即在官方客户端中重新登录/撤销相关会话，并清理 Git 历史；仅删除当前文件并不能让历史中的令牌失效。

## 设计边界

本项目不提供服务器端代理，也不应被用于访问他人账户、绕过权限或规避速率限制。它依赖 Codex 本地登录态和非公开接口，接口或认证格式变化时可能失效。
